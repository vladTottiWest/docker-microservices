name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout коду
        uses: actions/checkout@v4

      # Чистий запуск збірки через PowerShell на твоєму ПК
      - name: Збірка Docker образу
        run: |
          cd backend
          docker build -t backend-api:latest .

      # Нативний деплой у локальний Kubernetes через Helm
      - name: Деплой через Helm-чарт
        run: |
          helm upgrade --install my-release ./my-app-chart --set image.repository=backend-api --set image.tag=latestі