server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Проксіювання запитів на backend сервіс всередині мережі Docker
    location /api/ {
        proxy_pass http://backend-api:3000/;
        proxy_http_version 1.1;
    }
}